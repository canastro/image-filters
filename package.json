{
  "name": "image-filters",
  "version": "0.0.2",
  "description": "* threshold filter * invert filter * sepia filter * tint filter",
  "main": "index.js",
  "scripts": {
    "build": "browserify sandbox/sandbox.js > sandbox/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^13.0.0",
    "image-filter-threshold": "0.0.1"
  },
  "devDependencies": {
    "image-brightness": "latest",
    "image-contrast": "latest",
    "image-filter-grayscale": "latest"
  }
}
